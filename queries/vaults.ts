/**
 * Vault query options: stats and units.
 *
 * The vault hooks have relatively simple query logic, so they're refactored
 * in-place to use queryKeys and remove polling config. This file is a placeholder
 * for any shared vault query utilities.
 */

export {};
