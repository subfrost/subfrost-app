# Environment Variables Example
# Copy this to .env.local and fill in your values

# =============================================================================
# PUBLIC SETTINGS (safe to expose in browser)
# =============================================================================
NEXT_PUBLIC_NETWORK=mainnet

# Google Drive OAuth Client ID for wallet backup/restore
# Optional: Uses default Client ID if not specified
# Get your own from Google Cloud Console (see /GOOGLE_CLOUD_SETUP.md)
# NEXT_PUBLIC_GOOGLE_DRIVE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# =============================================================================
# DATABASE (Cloud SQL PostgreSQL) - Server-side only
# =============================================================================
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# For Cloud Run with private IP (production):
# DATABASE_URL="postgresql://subfrost_app:YOUR_PASSWORD@10.11.192.3:5432/subfrost"

# For local development with Docker:
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/subfrost"

# =============================================================================
# CACHE (Memorystore Redis) - Server-side only
# =============================================================================
# For Cloud Run with private IP (production):
# REDIS_HOST=10.11.193.4

# For local development:
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD= # Not required for local or Memorystore via private VPC
